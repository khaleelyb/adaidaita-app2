# Adaidaita Backend

This backend is a minimal starter for the Adaidaita app. It provides:
- Trip creation & acceptance
- A simple webrtc/ice endpoint returning TURN config
- OneSignal notification sender

## Local development
1. Copy `.env.example` to `.env` and fill values.
2. `cd backend && npm install`
3. `npm run dev`
